{
  "name": "valet-sdk-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo for Valet's browser + React Native SDKs",
  "license": "MIT",
  "workspaces": ["packages/*"],
  "scripts": {
    "build":     "npm run build --workspaces --if-present",
    "test":      "npm test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
